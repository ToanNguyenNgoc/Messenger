import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  cnt: {
    width: 32,
    height: 32,
    padding: 0,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 100,
  },
});
