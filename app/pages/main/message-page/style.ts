import {color} from '@/themes';
import {StyleSheet} from 'react-native';

export const styleHead = StyleSheet.create({
  cnt: {
    paddingVertical: 6,
  },
  headLeft: {
    alignItems: 'center',
  },
});
